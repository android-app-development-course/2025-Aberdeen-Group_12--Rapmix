package com.example.rapmix.ui

import androidx.compose.ui.graphics.Color

// ===== RAPmix Palette (Cyber / Neon / Music) =====

// App Background gradient (top -> mid -> bottom)
val BgTop = Color(0xFF0B1020)
val BgMid = Color(0xFF121A2F)
val BgBottom = Color(0xFF0A0F1C)

// Surfaces / cards
val CardBg = Color(0xFF1A2238)
val CardBgElevated = Color(0xFF222C45)

// Neon accents
val NeonPurple = Color(0xFFC84BFF) // primary accent
val NeonPink = Color(0xFFFF5AF7)   // secondary accent

// Text hierarchy
val TextTitle = Color(0xFFF4E8FF)
val TextSection = Color(0xFFD66BFF)
val TextBody = Color(0xFFC8CDE7)
val TextSub = Color(0xFF8B91B3)
val TextDisabled = Color(0xFF5F6485)

// UI lines / separators
val DividerLine = Color(0x332B3150) // subtle
